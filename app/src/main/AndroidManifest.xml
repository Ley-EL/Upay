<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.upay">
    <!-- Declare Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Upay">
        <activity
            android:name=".Models.ProductsDetailsModel"/>
        <activity
            android:name=".WishList"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".UserConnections.LogOut" />
        <activity android:name=".extra.stepper.Stepper_three_Activity" />
        <activity android:name=".extra.stepper.Stepper_two_Activity" />
        <activity android:name=".extra.stepper.Stepper_one_Activity" />
        <activity android:name=".UserConnections.LoginActivity" />
        <activity android:name=".UserConnections.RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>